# presets/

Shipped preset files, for when factory presets move out of code (see
`plans` N7). Empty for now: every factory preset is compiled in from
`modules/<id>/presets/FactoryPresets.h` and `products/rack/Product.h`.
