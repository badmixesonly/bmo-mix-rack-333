# BMO DEQ — the serial-vs-parallel blind run, 2026-09-11

The run record for the listening pass that `deq-topology-listening.md`
describes. That file is the method and the argument; this one is what was
actually rendered, from what, and what the tool measured while it did it.

**Rendered on AURORA**, 2026-09-11, from `add-bmo-deq`, with
`measure_deq render ... --blind 7` (DSP-only Release build). The DSP is the
shipping one: the tool drives `DspCore`, not a stand-in.

**Where the files are:** `C:\Users\thesp\deq-blind\<source>\`, outside the
repository. They are 32-bit float WAVs named `<case>-A.wav` / `<case>-B.wav`,
with `key.txt` beside them and `ANSWERS.md` at the top of the folder. **The
key stays shut until every line of ANSWERS.md has something on it.** Nobody in
this session has opened one.

## Sources

Frosty's brief: BMO DEQ is meant to be a workhorse, so the question is not
whether serial holds up on one vocal but whether anything about it is worse on
anything anyone would reach for it with.

| # | source | what it covers | rendered |
|---|---|---|---|
| 1 | Failure, dry vocal (Tune shoot-out) | a real vocal: sibilance, chest, phrase gaps | yes |
| 2 | PHRYGIAN D 808 | sustained low end, where stacked cuts and phase matter most | yes |
| 3 | PHRYGIAN D drum loop | transients, and a full-band source for the dynamic cases | yes |
| 4 | PHRYGIAN D synth | mid-dense and harmonically busy, where curves overlap | yes |
| 5 | DEQ ref GTR | guitars | **no — the export is silent** |
| 6 | DEQ ref ROOM | a room source with real top end | **no — the export is silent** |
| 7 | a full mix | everything at once, the case a mix engineer meets | not supplied yet |

### The two silent exports

`DEQ ref GTR.wav` and `DEQ ref ROOM.wav` contain no audio. Both are
well-formed 32-bit float WAVs, 44.1 kHz, stereo, 14.9 s, about 5 MB each — and
every sample in them is zero. Checked two ways: `measure_deq` reads both as
−300 dBFS RMS and −300 dBFS peak, and scanning the files past their headers
finds not one non-zero byte. The same reader handles the other three files
from the same folder, whose headers are identical, so this is the bounce
rather than the tool.

The usual causes are a muted or un-armed track, a solo left up somewhere else,
or a render range that landed on empty bars. Re-export those two and they can
be rendered in a minute; nothing else has to change.

## What the tool measured, per source

`serial vs parallel` is the worst difference between the two renders, relative
to the input: the more negative, the more alike they are. `max GR` is how hard
the dynamic bands actually worked **on that material** — a case reading 0.0
never engaged, so its pair is two static filters and says nothing about
dynamics.

### 1. Failure, dry vocal — 44.1 kHz, 19.3 s, RMS −21.8 dBFS, peak −4.2 dBFS

| # | case | serial vs parallel | serial peak | parallel peak | max GR |
|---|---|---|---|---|---|
| 1 | `lowcut-under-shelf` | −26.7 dB | −4.1 | −4.1 | — |
| 2 | `stacked-cuts-12` | −7.3 dB | −13.5 | −9.2 | — |
| 3 | `stacked-cuts-6` | −15.6 dB | −11.1 | −10.4 | — |
| 4 | `stacked-boosts` | −17.0 dB | **−0.0** | −0.5 | — |
| 5 | `surgical` | −31.0 dB | −5.0 | −4.9 | — |
| 6 | `vocal-control` | −32.5 dB | −3.8 | −3.7 | — |
| 7 | `deess-dynamic` | −37.1 dB | −4.2 | −4.2 | **10.0 dB** |
| 8 | `tamer-dynamic` | −29.8 dB | −4.1 | −4.0 | **10.1 dB** |

Both dynamic cases engaged fully. Case 4 renders to −0.0 dBFS: turn down
first. Case 2's 4.3 dB of peak difference is the topology, not a fault — two
−12 dB cuts stack to −24 dB in serial and invert to −6 dB in parallel.

### 2. PHRYGIAN D 808 — 44.1 kHz, 7.3 s, RMS −18.2 dBFS, peak −11.2 dBFS

| # | case | serial vs parallel | serial peak | parallel peak | max GR |
|---|---|---|---|---|---|
| 1 | `lowcut-under-shelf` | **+1.3 dB** | −9.6 | −13.7 | — |
| 2 | `stacked-cuts-12` | −29.7 dB | −11.3 | −11.1 | — |
| 3 | `stacked-cuts-6` | −38.6 dB | −11.3 | −11.3 | — |
| 4 | `stacked-boosts` | −35.0 dB | −11.2 | −11.1 | — |
| 5 | `surgical` | −48.5 dB | −11.2 | −11.2 | — |
| 6 | `vocal-control` | −40.6 dB | −13.3 | −13.5 | — |
| 7 | `deess-dynamic` | −85.9 dB | −11.2 | −11.2 | 0.9 dB |
| 8 | `tamer-dynamic` | −281.8 dB | −11.2 | −11.2 | 0.0 dB |

**This is the source that makes case 1 the argument it is.** The difference
between the two topologies is *louder than the input itself* (+1.3 dB), and
4.1 dB separates their peaks: serial's low cut actually cuts under the shelf,
while parallel's is bypassed by the shelf's own path. On a sustained 808 that
is not a subtlety. Everything else here sits far below audibility, and the
dynamic cases barely moved — a near-sine low source gives a de-esser nothing
to do.

### 3. PHRYGIAN D drum loop — 44.1 kHz, 7.3 s, RMS −25.5 dBFS, peak −9.9 dBFS

| # | case | serial vs parallel | serial peak | parallel peak | max GR |
|---|---|---|---|---|---|
| 1 | `lowcut-under-shelf` | **−2.0 dB** | −9.6 | −10.2 | — |
| 2 | `stacked-cuts-12` | −12.8 dB | −11.8 | −9.8 | — |
| 3 | `stacked-cuts-6` | −21.2 dB | −11.7 | −11.1 | — |
| 4 | `stacked-boosts` | −10.4 dB | −4.4 | −5.4 | — |
| 5 | `surgical` | −23.7 dB | −10.3 | −10.5 | — |
| 6 | `vocal-control` | −33.9 dB | −9.6 | −9.6 | — |
| 7 | `deess-dynamic` | −33.0 dB | −10.2 | −10.1 | **7.5 dB** |
| 8 | `tamer-dynamic` | −26.1 dB | −9.5 | −9.6 | **8.7 dB** |

The most useful source in the set: case 1 is 2 dB from the input, the stacked
cases are all live, and both dynamic cases engaged properly on transients —
which is the hardest thing to get right, and where a topology difference would
show as pumping or smearing rather than as tone.

### 4. PHRYGIAN D synth — 44.1 kHz, 7.3 s, RMS −29.9 dBFS, peak −15.3 dBFS

| # | case | serial vs parallel | serial peak | parallel peak | max GR |
|---|---|---|---|---|---|
| 1 | `lowcut-under-shelf` | −6.8 dB | −14.0 | −16.0 | — |
| 2 | `stacked-cuts-12` | −10.3 dB | −18.4 | −16.8 | — |
| 3 | `stacked-cuts-6` | −18.5 dB | −18.2 | −17.7 | — |
| 4 | `stacked-boosts` | −12.0 dB | −9.3 | −10.3 | — |
| 5 | `surgical` | −25.4 dB | −16.1 | −16.1 | — |
| 6 | `vocal-control` | −32.6 dB | −14.9 | −14.9 | — |
| 7 | `deess-dynamic` | −67.1 dB | −15.3 | −15.3 | 0.9 dB |
| 8 | `tamer-dynamic` | −44.3 dB | −14.1 | −14.1 | 1.9 dB |

Mid-dense and sustained: cases 1 to 4 all sit within 12 dB of the input, so
this is where stacked curves that do not add should be audible as tone. The
dynamic cases barely engaged.

## Where the dynamics are actually tested

Only two sources drive the dynamic bands hard enough to mean anything: the
**vocal** (10.0 / 10.1 dB) and the **drum loop** (7.5 / 8.7 dB). On the 808
and the synth, cases 7 and 8 are effectively two static filters — listen if
curious, but they answer nothing about dynamics, and
`deq-topology-listening.md` says to note that rather than read a verdict into
it.

## Before you listen

- **Check the level.** `stacked-boosts` hits −0.0 dBFS on the vocal and
  −4.4 dBFS on the drums. Same gain across A and B of a pair, always.
- **Play a pair back to back**, not a bank of files. The question is per case.
- **Do `vocal-control` first, on every source.** It is the validity check: the
  two renders differ by −32 to −41 dB relative to input, which should be
  inaudible. If that pair is distinguishable, something other than the
  topology is being heard, and nothing else from that source can be trusted.
- **A and B are shuffled per case**, so preferring "A" twice means nothing.

## What the answer decides

Serial was chosen on the measurements (`spec/topology-options.md`) and every
test that can be written is written: the response is the band curves added in
dB, a low cut still cuts under a boost, static bands do not depend on their
order. What is left is whether the difference matters on real material, and
whether anything about serial is worse.

If serial holds, `DspCore.h`'s `Topology` default stays where it is and spec
C4 is revised in the same commit. **Whether a serial/parallel switch is ever
exposed as a control is a separate question, held open until this pass is
heard** (Frosty, 2026-09-11) — and it is the one thing still keeping DEQ's
panel out of the UI pass.

## Picking this up on the other machine

The work is on the fork; the audio is not. What does not travel, and what to
do about it:

**The renders.** They were made on AURORA at `C:\Users\thesp\deq-blind\`,
outside the repository. Either copy the folder (about 40 MB a source), or
**re-render**: `measure_deq render` is deterministic — same source, same
`--blind 7`, same build gives byte-identical files and the same A/B shuffle —
so only the source wav has to travel.

**The blank answers sheet does travel**:
`testing-notes/deq-blind-answers.md` is the copy in git. Put it beside the
audio as `ANSWERS.md`, fill it in there, and commit the filled copy back over
the one in `testing-notes/` when the pass is done.

```
git worktree add -b deq-work ../bmo-mix-rack-333-deq origin/integration
cmake -S . -B build-dsp -DBMO_DSP_ONLY=ON
cmake --build build-dsp --config Release --target measure_deq
build-dsp/tools/Release/measure_deq.exe render <source.wav> <short-out-folder> --blind 7
```

A listening-only pass needs neither the licensed fonts nor the JUCE submodule:
those are for the panel, not the renderer. **Keep the output path short** —
Windows stops at 260 characters, and the tool refuses a folder it cannot use
rather than write half a set. **Name the machine** in anything written down.

**Two machines, two branches.** While DEQ is worked on here, Tune is on
`bmo-tune-work`. Both merge into `integration`, so fetch before merging and
never rebase a branch the other machine has.
